// export function getPostBySlug(slug: string) {
//   const paths = getAllFileNamesFrom(postsDir)
//   const result = paths.find(path => path.includes(slug))

//   if (!result) {
//     return null
//   }

//   const post = getPostByPath({ dateStyle: 'long' })(result)
//   return post as Post
// }
